<Page x:Class="AlgebraOfSignatures.WPF.View.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:AlgebraOfSignatures.WPF.View.Pages"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:dataGridDescription="clr-namespace:AlgebraOfSignatures.WPF.Converters.DataGridDescription"
      xmlns:controls="clr-namespace:AlgebraOfSignatures.WPF.Controls"
      mc:Ignorable="d"
      Title="CatalogPage"
      FontSize="{StaticResource MainFontSize}">
    <Page.Resources>
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            
            <Setter Property="Foreground" Value="#111827"/>
        </Style>
        <Style TargetType="xctk:IntegerUpDown">
            <Setter Property="Width" Value="195"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="BorderBrush" Value="#94A3B8"/>
            <Setter Property="Background" Value="#FFFFFF"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="195"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"
                Background="#FFFFFF"
                BorderBrush="#94A3B8"
                BorderThickness="1.3"
                CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <WrapPanel Grid.Column="0">
                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="Количество вершин"/>
                        <xctk:IntegerUpDown 
                            Value="{Binding VertexCount,
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                            Minimum="{Binding UniformityDegree,
                                Mode=OneWay,
                                UpdateSourceTrigger=PropertyChanged}"
                            Padding="0, 0, 4, 0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="Степень однородности"/>
                        <xctk:IntegerUpDown 
                            Minimum="2"
                            Maximum="{Binding VertexCount,
                                    Mode=OneWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                            Value="{Binding UniformityDegree, 
                                    Mode=TwoWay,
                                    UpdateSourceTrigger=PropertyChanged}"
                            Padding="0, 0, 4, 0"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,30,0">
                        <TextBlock Text="Отобразить как"/>
                        <ComboBox 
                            ItemsSource="{Binding RepresentationTypesValues,
                                        Mode=OneWay,
                                        UpdateSourceTrigger=PropertyChanged}"
                            SelectedItem="{Binding SelectedRepresentationType,
                                       Mode=TwoWay,
                                       UpdateSourceTrigger=PropertyChanged}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Text="{Binding Converter={dataGridDescription:RepresentationTypeToDescriptionConverter}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                </WrapPanel>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding UniformHyperGraphsList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:UniformHyperGraphControl
                            Margin="0,10,40,0"
                            IsReadOnly="True"
                            UniformHyperGraph="{Binding}"
                            RepresentationToVisualize="{Binding
                        DataContext.SelectedRepresentationType,
                        RelativeSource={RelativeSource AncestorType=Page}}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
